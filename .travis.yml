addons:
  apt:
    packages:
    - gdal-bin
    - libgdal-dev
before_script:
    - npm run setup-gadm;
    - npm run setup-mapspam;
cache:
  directories:
    - "node_modules"
language: node_js
node_js:
  - "node"
  - "lts/*"
